import { Button } from "@fieldmade/ui";

export default function Index() {
  return (
    <div className="h-screen w-screen flex justify-center items-center">
      <Button variation="primary">Hello World</Button>
    </div>
  );
}
